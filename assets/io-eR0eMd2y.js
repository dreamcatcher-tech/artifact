const e={relay:{description:"Returns the last tool call results and ends the AI conversation.  Useful where one AI is calling another AI and you want to relay the results back to the original caller without consuming any extra tokens or effect from the executing AI.",type:"object",additionalProperties:!1,properties:{}}},t={relay:async()=>"@@ARTIFACT_RELAY@@"};export{e as api,t as functions};
